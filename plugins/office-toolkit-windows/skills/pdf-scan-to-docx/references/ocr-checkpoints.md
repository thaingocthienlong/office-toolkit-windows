# OCR checkpoints

Read rendered images in page order. Save one UTF-8 Markdown file per page as `02.process\ocr\page-####.md`; preserve Vietnamese Unicode, headings, lists, tables, and unreadable text as `[Không đọc được]`.

Do not invent text, copy images outside the workspace, or renumber pages. Resume by leaving completed checkpoints unchanged and adding only missing pages.
